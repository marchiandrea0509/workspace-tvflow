## JDUSDT B1/B2 6x RED-override cancel/replace

- Andrea explicitly instructed cancellation of previous JD live orders and placement of B1 `28.16 @30.54`, SL `29.12`, TP `32.00`, plus B2 `157.89 @29.50`, SL `29.12`, TP `30.54`, with RED-liquidity override.
- Precheck found one prior JD buy/open limit live and unfilled: orderId `1462722420056690689`, clientOid `oc-jd-a1-6x-red-20260719-A1`, `151.51 @29.23`, preset SL `28.57`, TP `30.27`, isolated `6x`. JD had no position and no normal/profit_loss/track plan rows.
- The supplied values reconciled: total qty `186.05`, notional `5517.7614 USDT`, planned risk `99.9854`, planned reward `205.3192`, aggregate RR `~2.05`. Contract precision accepts `0.01` size and price increments. JD account state was already isolated long `6x`, so that setting was preserved; simple margin is `919.6269 USDT`.
- Fresh full-ladder liquidity gate was RED: stop-exit simulated slippage, near-market depth, and spread were GREEN; p10 weak-minute activity, dead candles, and 24h turnover were RED. Andrea's explicit RED override was applied with thin-turnover/stop-slippage acknowledgement.
- Canceled old order `1462722420056690689` successfully and verified JD regular/normal/profit_loss/track counts `0/0/0/0`, with no position, before replacement.
- Placed B1 orderId `1463355134707404801` / `oc-jd-brokenpwh-6x-red-20260721-B1`, buy/open limit `28.16 @30.54`, preset SL `29.12`, TP `32.00`, isolated `6x`.
- Placed B2 orderId `1463355230715023362` / `oc-jd-brokenpwh-6x-red-20260721-B2`, buy/open limit `157.89 @29.50`, preset SL `29.12`, TP `30.54`, isolated `6x`.
- Postcheck: both orders live/unfilled (`baseVolume=0`), exact preset SL/TP attached, no JD position and no separate plan rows. Account union available after placement `942.00999946 USDT`. Post-fill liquidity monitors PIDs `9132` and `12736`, common prefix `oc-jd-brokenpwh-6x-red-20260721`.
- Journal refreshed NoSend: `reports/trade_journal/bitget_futures_trade_report_2026-07-21T06-57-21-472Z.xls` (`openOrders=5`, `positions=3`, `netPnlEst=873.9845186`). Execution summary: `bitget-futures-harness/reports/live_execution/20260721_jdusdt_broken_pwh_b1_b2_6x_red_replace/summary.md`.

## JD delivery repair and mandatory post-live finalizer

- Andrea flagged that the JD live-order response again omitted the TradingView price lines and a delivered journal entry. This repeated the same visible-delivery regression seen on recent SOXL/JD runs, so it was treated as a workflow defect rather than a one-off follow-up.
- TradingView Desktop MCP drew the current live JD ladder on `BITGET:JDUSDT.P` 4H with solid lines: B1 Entry `30.54`, SL `29.12`, TP `32.00`; B2 Entry `29.50`, SL `29.12`, TP `30.54`; one white group arrow at placement price `30.98`. Entity IDs: B1 Entry `aoo6Tx`, arrow `x5hv7w`, B1 SL `VmI9xr`, B1 TP `2KRaQv`, B2 Entry `2gdRur`, B2 SL `TVvj3H`, B2 TP `JWnPSp`. Screenshot `reports/tv_mcp_snapshots/20260721_093201/screenshot.png` visually verified the 4H chart and drawn segments.
- Journal rebuilt at 09:33 CEST to `reports/trade_journal/bitget_futures_trade_report_2026-07-21T07-32-37-040Z.xls`. Discord summary posted as message `1529028764061925448`, but the old five-message delivery loop stalled before the active-order row. Direct Discord read verified only the summary; stopped orphan sender PIDs `13236` and `1480`, then posted a dedicated JD B1/B2 journal entry successfully as message `1529029921844760676`.
- Added `scripts/finalize_bitget_live_order_workflow.ps1` as the mandatory post-live hard gate. After exchange postchecks it validates the live plan, draws TradingView solid Entry/SL/TP plus arrow, saves a screenshot, refreshes the journal, requires a Discord message ID, and writes a machine-readable receipt. A normal live workflow cannot be called complete without both receipts; `-NoSend` remains diagnostic only.
- Patched `scripts/run_bitget_journal_update.ps1` with `-DeliveryProfile live-order` (one atomic Discord message combining summary + active orders) and `-ReceiptOut` for automation. Updated workspace `AGENTS.md`, harness README, execution-service README, and `tvflow-fast` AGENTS so both full and fast execution paths require the finalizer.
- Validation: PowerShell parser PASS for both scripts; finalizer `-ValidateOnly` PASS for `BITGET:JDUSDT.P` 4H with 2 orders; journal live-order profile NoSend/receipt PASS at `reports/live_execution/workflow_receipts/20260721_journal_profile_validation.json`; `git diff --check` PASS.
