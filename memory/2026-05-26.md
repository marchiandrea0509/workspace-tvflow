# 2026-05-26 � tvflow durable memory

## COPPERUSDT deep-analysis discrepancy feedback
- Andrea compared tvflow's COPPERUSDT.P deep analysis against GPT's screenshot prompt/output.
- Root cause: tvflow anchored A/B to a too-local confirmed 4H impulse (`6.31 -> 6.503`) while price was near major `6.49-6.503` resistance; GPT used the broader visible 4H swing (`~6.158 -> 6.503`), which produced better pullback levels (`6.365 / 6.330 / 6.300`) and structural SL (`6.238`).
- tvflow was still right on exchange/live orderability: rerunning the live Bitget liquidity gate for GPT-style A/B ladders stayed RED (24h quote volume far below 500x notional, dead 1m candles ~46.7%, p10 1m quote volume zero/unavailable, no size-only compliant version).
- Durable patch: `prompts/master_trade_analysis_prompt_v2.md` and `README_DEEP_ANALYSIS_V2.md` now require broad visible 4H swing anchoring near major R/S and require reports to separate chart-valid WATCH tickets from live orderability blockers.
- Feedback record: `reports/deep_analysis_feedback/20260526T062316Z_COPPERUSDT_feedback.json`.

## COPPERUSDT alert-only virtual OCO monitor
- Andrea confirmed the COPPER VOCO should be alert-only: no live exchange order placement by OpenClaw; send a Discord DM when a branch triggers.
- Created `scripts/check_copper_voco_virtual.py`; it checks public Bitget ticker, recent 1m lows, and latest closed 4H candles, and emits one alert only.
- Cron job `d989a34f-a9b4-41d3-adb3-afb9478636b2` runs every 60s with Discord DM delivery to `user:1322306175865323552` and removes itself after an alert.
- State file: `reports/live_execution/virtual_oco/copper_voco_alert_state.json`.
- B triggers on price/recent 1m low <= `6.365`; C triggers only after latest closed 4H close > `6.503`, with max current entry `6.530`; if C confirms above max, alert says no-chase/manual review.

## COPPERUSDT VOCO watchdog correction / reuse existing tool
- Andrea correctly noted a reusable virtual OCO watchdog already existed and should be reused instead of creating a per-symbol script.
- Found existing docs/tooling: `README_VIRTUAL_OCO_WATCHDOG.md`, `scripts/virtual_oco_watchdog.js`, `scripts/run_virtual_oco_watchdog.ps1`, and `watchdog/virtual_oco_watchdog.template.json`.
- Removed ad-hoc COPPER 60s cron `d989a34f-a9b4-41d3-adb3-afb9478636b2`.
- Created config `watchdog/virtual_oco_COPPERUSDT_20260526_BC.json` for B pullback + C breakout alert-only VOCO.
- Scheduled reusable watchdog cron `897c8c2b-7c20-4e7a-a650-4adc9b69e7b3` at `5 0,4,8,12,16,20 * * * UTC`, so it checks 5 minutes after each fully closed 4H candle and never from intrabar wicks.
- Dry-run output: `NO_TRIGGER`; state path `reports/watchdogs/virtual_oco/VOCO_COPPERUSDT_20260526_BC.state.json`.
- Future VOCO alert-only work should use the generic config+watchdog path first; do not create bespoke monitors unless the reusable tool cannot express the requirement. Also removed the bespoke `scripts/check_copper_voco_virtual.py` and its ad-hoc state file after migrating to the reusable watchdog.

## VOCO watchdog short feedback patch
- Andrea asked the reusable virtual OCO watchdog to print short feedback every time it checks instead of staying silent on no-trigger checks.
- Patched `scripts/virtual_oco_watchdog.js` with `feedbackOnEveryCheck` / `printFeedbackEveryCheck` config support and wrapper `-Feedback` in `scripts/run_virtual_oco_watchdog.ps1`.
- No-trigger checks now print `VIRTUAL OCO CHECK � no trigger` with current price, checked candle, and the first blocking gate for each family; alert/expiry/invalidation behavior remains unchanged and alert-only.
- Updated `watchdog/virtual_oco_watchdog.template.json`, `README_VIRTUAL_OCO_WATCHDOG.md`, and enabled `feedbackOnEveryCheck: true` in `watchdog/virtual_oco_COPPERUSDT_20260526_BC.json`.
- Validation: `node --check scripts/virtual_oco_watchdog.js` passed; COPPER watchdog printed concise no-trigger feedback for the checked 4H candle.
# 2026-05-26 � tvflow durable memory

## COPPERUSDT deep-analysis discrepancy feedback
- Andrea compared tvflow's COPPERUSDT.P deep analysis against GPT's screenshot prompt/output.
- Root cause: tvflow anchored A/B to a too-local confirmed 4H impulse (`6.31 -> 6.503`) while price was near major `6.49-6.503` resistance; GPT used the broader visible 4H swing (`~6.158 -> 6.503`), which produced better pullback levels (`6.365 / 6.330 / 6.300`) and structural SL (`6.238`).
- tvflow was still right on exchange/live orderability: rerunning the live Bitget liquidity gate for GPT-style A/B ladders stayed RED (24h quote volume far below 500x notional, dead 1m candles ~46.7%, p10 1m quote volume zero/unavailable, no size-only compliant version).
- Durable patch: `prompts/master_trade_analysis_prompt_v2.md` and `README_DEEP_ANALYSIS_V2.md` now require broad visible 4H swing anchoring near major R/S and require reports to separate chart-valid WATCH tickets from live orderability blockers.
- Feedback record: `reports/deep_analysis_feedback/20260526T062316Z_COPPERUSDT_feedback.json`.

## COPPERUSDT alert-only virtual OCO monitor
- Andrea confirmed the COPPER VOCO should be alert-only: no live exchange order placement by OpenClaw; send a Discord DM when a branch triggers.
- Created `scripts/check_copper_voco_virtual.py`; it checks public Bitget ticker, recent 1m lows, and latest closed 4H candles, and emits one alert only.
- Cron job `d989a34f-a9b4-41d3-adb3-afb9478636b2` runs every 60s with Discord DM delivery to `user:1322306175865323552` and removes itself after an alert.
- State file: `reports/live_execution/virtual_oco/copper_voco_alert_state.json`.
- B triggers on price/recent 1m low <= `6.365`; C triggers only after latest closed 4H close > `6.503`, with max current entry `6.530`; if C confirms above max, alert says no-chase/manual review.

## COPPERUSDT VOCO watchdog correction / reuse existing tool
- Andrea correctly noted a reusable virtual OCO watchdog already existed and should be reused instead of creating a per-symbol script.
- Found existing docs/tooling: `README_VIRTUAL_OCO_WATCHDOG.md`, `scripts/virtual_oco_watchdog.js`, `scripts/run_virtual_oco_watchdog.ps1`, and `watchdog/virtual_oco_watchdog.template.json`.
- Removed ad-hoc COPPER 60s cron `d989a34f-a9b4-41d3-adb3-afb9478636b2`.
- Created config `watchdog/virtual_oco_COPPERUSDT_20260526_BC.json` for B pullback + C breakout alert-only VOCO.
- Scheduled reusable watchdog cron `897c8c2b-7c20-4e7a-a650-4adc9b69e7b3` at `5 0,4,8,12,16,20 * * * UTC`, so it checks 5 minutes after each fully closed 4H candle and never from intrabar wicks.
- Dry-run output: `NO_TRIGGER`; state path `reports/watchdogs/virtual_oco/VOCO_COPPERUSDT_20260526_BC.state.json`.
- Future VOCO alert-only work should use the generic config+watchdog path first; do not create bespoke monitors unless the reusable tool cannot express the requirement. Also removed the bespoke `scripts/check_copper_voco_virtual.py` and its ad-hoc state file after migrating to the reusable watchdog.

## VOCO watchdog short feedback patch
- Andrea asked the reusable virtual OCO watchdog to print short feedback every time it checks instead of staying silent on no-trigger checks.
- Patched `scripts/virtual_oco_watchdog.js` with `feedbackOnEveryCheck` / `printFeedbackEveryCheck` config support and wrapper `-Feedback` in `scripts/run_virtual_oco_watchdog.ps1`.
- No-trigger checks now print `VIRTUAL OCO CHECK � no trigger` with current price, checked candle, and the first blocking gate for each family; alert/expiry/invalidation behavior remains unchanged and alert-only.
- Updated `watchdog/virtual_oco_watchdog.template.json`, `README_VIRTUAL_OCO_WATCHDOG.md`, and enabled `feedbackOnEveryCheck: true` in `watchdog/virtual_oco_COPPERUSDT_20260526_BC.json`.
- Validation: `node --check scripts/virtual_oco_watchdog.js` passed; COPPER watchdog printed concise no-trigger feedback for the checked 4H candle.

## APPUSDT.P deep analysis
- Andrea requested a fresh deep analysis on `BITGET:APPUSDT.P`.
- Built packet `reports/deep_analysis_packets_v2/20260526_153824_APPUSDT/` and report `reports/deep_analysis/2026-05-26_APPUSDT_deep_analysis.md`.
- Verdict: `GOOD MARKET / BAD MARKET ENTRY / LIVE ORDERABILITY BLOCKED`.
- Chart read: APP was in a fast bullish breakout attempt from the `475-480` base through `504.93 / 514.61 / 522.75`, with impulse high around `534.44` and next visible daily resistance around `571.59`; 1H/4H move was vertical, so market buy/chase was rejected.
- Best chart-valid A: single pullback long watch only at `504.93`, SL `492.50`, TP `534.44`, qty about `8.04`, risk about `99.94 USDT`, margin about `1015 USDT` at 4x, R:R about `2.37`.
- B fill-probability pullback rejected: shallow `522.75 / 514.61` levels were too chase-like or weak once the structural SL had to stay below `492.50`; better to keep A as a single high-quality limit.
- C breakout conditional watch only: valid only after a closed 4H candle above `534.44`, max entry around `536.15`, SL `514.50`, TP `571.59`, qty about `4.62`, R:R about `1.64`; reject if price is already beyond max chase.
- Read-only Bitget state during analysis: APP regular orders `0`, plan orders `0`, no APP position observed.
- Live orderability: liquidity gate approximations were RED due thin RWA liquidity (24h volume too low vs 500x notional, dead 1m candles around 70%, spread/depth issues, no size-only compliant version). Therefore chart-valid tickets are `WATCH_ONLY / NOT_LIVE_PLACEABLE` unless Andrea separately requests live action and explicitly accepts RED liquidity risk.

## APPUSDT GPT comparison / corrected pullback plan
- Andrea challenged the APPUSDT liquidity/orderability explanation and preferred GPT's APP proposal.
- Correction: GPT's chart proposal was better. tvflow correctly rejected market-buy/chase but over-filtered future conditional pullback limits by applying current live top-book/liquidity snapshots too heavily.
- Correct split: chart/order geometry vs live execution risk. For conditional retest limits, current spread/depth should be treated as risk context and rerun at trigger/fill time, not used to erase chart-valid pullback watch tickets.
- The massive APP volume spike was real (current 4H candle around ~545k USDT quote volume, 24h quote around ~766k), but strict 500x notional gate still marked 24h volume low: GPT A notional ~4541 requires ~2.27m, GPT B notional ~10177 requires ~5.09m. Thus chart volume confirmed momentum, while strict exchange liquidity remained thin.
- Rerun liquidity on GPT A/B: depth-to-SL was OK for both; spread remained BLOCK around ~0.19-0.23%, p10 1m quote volume was zero due inactive minutes in lookback, and 24h volume was below strict threshold. Future runs should state exactly which gate is chart-invalidating vs live-execution-risk only.
- Corrected report: `reports/deep_analysis/2026-05-26_APPUSDT_deep_analysis_corrected.md`.
- Feedback record: `reports/deep_analysis_feedback/20260526T144236Z_APPUSDT_feedback.json`.
- Corrected chart verdict: A valid conditional pullback `515 / 505`, SL `496.20`; B valid conditional/pref practical pullback `522.75 / 515`, SL `511.30`; C breakout rejected; no VOCO needed. Wait for 4H close above `522.75` before preparing B; if it rejects below `522.75`, wait for A-level pullback.

## COPPERUSDT VOCO delivery fix
- Andrea asked why no VOCO feedback arrived after candle close. Cron runs for `897c8c2b-7c20-4e7a-a650-4adc9b69e7b3` did execute and produced `VIRTUAL OCO CHECK � no trigger` summaries at 12:05 and 16:05 UTC, but `deliveryStatus` was `not-delivered` because Discord DM announce delivery to `dm:1322306175865323552` failed/fell back unsuccessfully.
- Current COPPER VOCO status at check: still `ARMED`; last checked candle start `2026-05-26T12:00:00Z`, close `6.399`, high `6.429`, low `6.383`, current around `6.40-6.412`; B waiting because zone `6.300-6.365` not touched, C waiting because close not above `6.503`.
- Fixed cron delivery target to the Bitget Trades channel `channel:1499631210283008002` and updated payload interpretation to surface `VIRTUAL OCO CHECK` messages, not only ALERT/INVALIDATED/EXPIRED.

## COPPERUSDT VOCO split routing confirmation
- Andrea requested split routing: routine `VIRTUAL OCO CHECK` status feedback in Bitget Trades room, but actual `VIRTUAL OCO ALERT` trigger to Discord DM.
- Updated COPPER VOCO cron `897c8c2b-7c20-4e7a-a650-4adc9b69e7b3`: top-level delivery stays `channel:1499631210283008002` for routine status; payload now routes `VIRTUAL OCO ALERT` by creating a one-shot DM alert job to `dm:1322306175865323552` with fallback/failureDestination back to `channel:1499631210283008002`.
- Important: do not promise DM is guaranteed. Historical DM route tests failed/unknown (`dm:` and `user:` variants); latest DM retry jobs failed before proving delivery. Future trigger alerts should have room fallback so a trigger is not silently lost if DM delivery fails.

## COPPERUSDT VOCO DM emulation test attempts
- Andrea asked to emulate a VOCO trigger to verify whether Discord DM fires.
- Created simulated trigger jobs with DM delivery and room fallback. One immediate Gemini-based test failed before proving delivery (`Agent couldn't generate a response`, `deliveryStatus: not-delivered`). Later duplicate future test jobs were removed to avoid spam.
- A single scheduled simulated trigger test job `f5ab74d7-66d9-46b8-9653-8daf3343a86f` remained queued around 2026-05-26T20:15Z at the time of reply. Do not claim DM route is proven until its run history shows `deliveryStatus: delivered` to `dm:1322306175865323552`.

## COPPER VOCO alert route hardened
- Root cause/evidence: Discord cron delivery to `dm:1322306175865323552` resolves as a DM but repeatedly finishes `deliveryStatus: not-delivered`; simulated trigger `f5ab74d7-66d9-46b8-9653-8daf3343a86f` produced the correct `VIRTUAL OCO ALERT` text but did not deliver, and configured `failureDestination` did not reliably rescue to the room.
- Proven route: Bitget Trades room `channel:1499631210283008002` delivered successfully for the 22:05 Berlin routine watchdog check and for simulated robust-route trigger job `e59119c3-69a1-48e9-a6e8-7ca4c13fb7ac` (`deliveryStatus: delivered`).
- Robust patch: COPPER VOCO cron `897c8c2b-7c20-4e7a-a650-4adc9b69e7b3` now sends routine `VIRTUAL OCO CHECK` statuses to Bitget Trades room and sends actual `VIRTUAL OCO ALERT` triggers to the same room with `<@1322306175865323552>` mention. This intentionally prioritizes not missing a trigger over DM privacy until OpenClaw cron DM delivery is fixed/proven.
- Removed temporary simulated test cron jobs after validation.

## APPUSDT feedback correction — GPT pullback plan was better
- Andrea challenged tvflow's APPUSDT.P deep analysis after comparing with GPT. Correction: GPT's chart construction was better; tvflow correctly rejected a market/chase buy, but over-filtered future conditional pullback limit plans by applying current live liquidity/orderbook snapshots too heavily.
- Correct split for future APP-style cases: chart/order geometry and live execution gate must be separated. A structurally valid future retest ladder can be WATCH_ONLY even if current Bitget RWA liquidity/spread/dead-candle metrics are RED; rerun liquidity at trigger/fill time.
- APP corrected chart plan: A best-quality pullback `515 / 505`, SL `496.20`; B preferred practical/fill-probability pullback `522.75 / 515`, SL `511.30`; C breakout/chase rejected. Wait for 4H close above `522.75` before B; if rejected below `522.75`, wait for deeper A zone.
- Volume interpretation lesson: APP's spike was real and bullish for chart context (fresh 4H volume around `545k USDT`, 24h quote around `766k USDT`), but strict execution gate still called 24h volume low because it expects ~500x planned notional (`~2.27m` for A, `~5.09m` for B). Do not phrase this as weak chart volume; phrase as bullish chart momentum but thin exchange execution liquidity.
- Saved corrected report: `reports/deep_analysis/2026-05-26_APPUSDT_deep_analysis_corrected.md`. Feedback record referenced in chat: `reports/deep_analysis_feedback/20260526T144236Z_APPUSDT_feedback.json`.

## COPPER VOCO watchdog delivery hardening
- Andrea asked why the active COPPER virtual OCO watchdog gave no candle-close feedback. Root cause found from observed delivery behavior: watchdog checks did run and produced feedback, but Discord DM delivery to `dm:1322306175865323552` / `user:1322306175865323552` was not reliable (`not-delivered` / failed or timed-out agent response). The room/channel route was proven to deliver.
- Desired routing clarified: routine watchdog status/no-trigger checks should be posted in the `#bitget-trades` Discord room/thread; actual VOCO trade triggers were initially desired by DM, but DM could not be proven reliable.
- Robust safety decision after emulated trigger testing: do **not** rely on DM-only for VOCO alerts. Actual `VIRTUAL OCO ALERT` triggers should be posted to the proven Discord room with `<@1322306175865323552>` mention so they cannot silently disappear. DM may be attempted only as a best-effort/private extra after it is proven, not as the sole critical alert path.
- Operational rule for future VOCO watchdogs: feedback and trigger alerts must use a proven visible route; critical trigger alert loss is worse than reduced privacy. If DM delivery is repaired later, validate with a simulated trigger and keep room fallback or dual-post until repeated DM success is observed.

## COPPER VOCO DM direct-tool fix after Andrea correction
- Andrea clarified mobile alerts are DM-only and reminded that on 2026-05-23 the working route was not cron runner delivery but a direct Discord message-tool send. Verified from old cron runs: job `e79b1ae6-a103-42b6-95dc-e0e5ff9fe452` had `delivery.mode=none`, payload instructed the agent to use the message tool, and run history shows `messageToolSentTo: [{ channel: discord, to: user:1322306175865323552 }]`; Andrea confirmed receiving that DM even though the run status had `Message failed`/not-requested delivery bookkeeping.
- Root cause refined: cron/runner announce delivery to `dm:1322306175865323552` is the broken/unreliable path. The direct in-agent Discord message-tool path to `user:1322306175865323552` is the route that worked and must be used for critical mobile alerts.
- Patched COPPER VOCO cron `897c8c2b-7c20-4e7a-a650-4adc9b69e7b3`: delivery mode now `none`, tools restriction cleared so the cron agent can use both `exec` and the Discord message tool. Payload runs the watchdog read-only with `-Json -Feedback -NoStateUpdate`, sends routine `VIRTUAL OCO CHECK` by message tool to `channel:1499631210283008002`, sends actual `VIRTUAL OCO ALERT` by message tool to `user:1322306175865323552` first, retries `dm:1322306175865323552` if needed, and only then commits watchdog state by rerunning without `-NoStateUpdate`. If notification cannot be attempted, state remains armed for retry.
- Validation: one-shot `VOCO DM direct-tool route test 20260526` run `55a9fa08-d558-4851-b645-5f8d1c99d87c` used messageToolSentTo `user:1322306175865323552` and completed ok with summary `?? VOCO DM route test 2026-05-26 22:40: direct message-tool path works. No trading action taken.` Manual run of the patched COPPER VOCO cron posted the routine status via messageToolSentTo `channel:1499631210283008002` and completed ok/not-requested runner delivery.

## COPPER VOCO DM direct-tool reception confirmed
- Andrea confirmed receiving the 2026-05-26 VOCO DM direct-tool route test. This proves the critical mobile-alert path works when the cron agent uses the Discord message tool directly to `user:1322306175865323552` with runner delivery disabled (`delivery.mode=none`).
- Committed the durable VOCO tool/routing modification in repo: README now documents the robust scheduled routing pattern, the template default target is `user:1322306175865323552`, and `PROJECT_STATE.md` records the root cause and patched cron behavior.
