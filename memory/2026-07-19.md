# 2026-07-19

Latest SOXLUSDT 2x RED-override live placement: Andrea initially requested nonexistent `SOXLUUSDT.P`, then corrected it to `SOXLUSDT.P`. The supplied short ladder was A1 `1.906 @150.12`, A2 `5.008 @159.12`, shared SL `171.10`, TP `116.70`, max margin `800 USDT`, with explicit RED-liquidity override. Bitget only permits `0.01` size increments, so quantities were rounded down to `1.90` and `5.00`. Fresh precheck found SOXL regular/normal/profit_loss/track `0/0/0/0` and no SOXL position. Selected isolated `2x`: total notional `1080.828`, simple margin `540.414`, exchange locked-margin increase `546.46987929`, below cap. Fresh full-ladder gate was YELLOW: stop-exit/depth/spread/24h GREEN, p10 RED, dead candles YELLOW; Andrea's RED override covered it. Placed live sell/open limits with preset SL/TP: S1 orderId `1462496974954205185` / `oc-soxl-short-2x-red-20260719-S1`, `1.90 @150.12`; S2 orderId `1462497132005724161` / `oc-soxl-short-2x-red-20260719-S2`, `5.00 @159.12`. Postcheck: both live/unfilled, isolated 2x, preset SL/TP attached, no SOXL position or plan rows. Total planned risk `99.762`, reward `275.598`, RR `~2.76`. Post-fill monitor PIDs `2892` and `10100`. Summary: `bitget-futures-harness/reports/live_execution/20260719_soxl_short_2x_red_override/summary.md`.

Journal hardening: a refresh with one explicit symbol and one current-position symbol exposed scalar string concatenation in `scripts/run_bitget_journal_update.ps1`, producing invalid `SOXLUSDTEWYUSDT` and omitting open orders. Patched the tracked-symbol merge to force both operands to arrays before concatenation. Validation refresh succeeded with separate `SOXLUSDT` and `EWYUSDT`; workbook `reports/trade_journal/bitget_futures_trade_report_2026-07-18T22-10-17-003Z.xls` showed `openOrders=3`, `positions=1`, `netPnlEst=874.41899032`.

SOXL follow-up after Andrea said journal/chart steps were missed: verified the latest workbook directly and confirmed both SOXL orders are present in its `Active Orders` table with order IDs `1462496974954205185` and `1462497132005724161`. Used the `tv-draw-trade-plan` workflow to draw `BITGET:SOXLUSDT.P` on 4H: two solid live short ladders with yellow entries `150.12/159.12`, red shared SL `171.10`, green shared TP `116.70`, and one white placement arrow at `133.52`; all draw calls returned success.

SOXL white-arrow correction: although the initial arrow entity returned success, TradingView snapped its tip to the start of the current 4H candle, hiding it behind the price bars. Removed only that arrow (`iDI2SW`) and created replacement `B2D36D` at refreshed current price `133.24`, pointing to the next 4H chart slot with width `4`; the six Entry/SL/TP lines were untouched. A fresh chart screenshot visually confirmed the thick white right-pointing arrow is visible beside the current price bars. Future current-price-arrow fixes may need the tip one chart slot to the right rather than at an intrabar timestamp that TradingView snaps backward.

JDUSDT A1 6x RED-override cancel/replace: Andrea requested canceling prior `JDZSDT` orders (treated as a typo for JDUSDT), placing A1 `151.51 @29.23`, SL `28.57`, TP `30.27`, max margin `800 USDT`, and explicitly overriding RED liquidity. Three prior JD orders were found; L1 had historical partial fill `0.82` but no current JD position/plan existed. Canceled all three and verified zero JD orders/position. Fresh gate RED on p10/dead/24h only; stop-exit, depth, spread GREEN. Set isolated `6x`; placed live/unfilled orderId `1462722420056690689` / `oc-jd-a1-6x-red-20260719-A1`. Exact notional `4428.6373`, planned risk `99.9966`, reward `157.5704`, RR `~1.58`; incremental locked margin `748.15774713`, below cap. Monitor PID `9312`; journal snapshot `reports/trade_journal/bitget_futures_trade_report_2026-07-19T13-03-20-562Z.xls`; summary `bitget-futures-harness/reports/live_execution/20260719_jdusdt_a1_6x_red_replace/summary.md`.

Live-tool safety hardening: a pre-patch `--help` invocation on `set-leverage.js` / `set-margin-mode.js` fell through to default BTCUSDT values and returned isolated `3x`; there was no BTC position and this matched the harness defaults. Patched both scripts so `--help` is read-only and added README warnings to always pass an explicit symbol. Future live-script inspection should read source or use only documented non-mutating help paths.

JD follow-up after Andrea flagged missing journal delivery and TradingView lines: the first journal refresh had used `-NoSend`, so it created the workbook but intentionally did not post. Regenerated at 15:10 CEST with active JD order `1462722420056690689`, `openOrders=2`, `positions=1`, `netPnlEst=874.4141966`. The subsequent wrapper send exposed a newer OpenClaw requirement for explicit `channel:<id>` targets; patched `scripts/run_bitget_journal_update.ps1` to normalize bare numeric channel IDs. Used the `tv-draw-trade-plan` workflow to draw `BITGET:JDUSDT.P` 4H: solid yellow Entry `29.23`, red SL `28.57`, green TP `30.27`, and white placement arrow `29.50`. All four draw operations returned success and screenshot `reports/tv_mcp_snapshots/20260719_150921/screenshot.png` visually confirmed the levels.
