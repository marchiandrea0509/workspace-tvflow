# 2026-06-10

## UNHUSDT deep-analysis correction and prompt patch
- Andrea flagged the UNHUSDT deep-analysis output as another format/usefulness failure: the reply collapsed the plan into a single leg and effectively ignored the expected A/B/C/D option framework, even though the saved report validator passed.
- GPT reference was better on structure/TP assignment: A quality pullback `398.27 / 393.00`, SL `389.00`, TPs `413.23 / 417.50`, aggregate RR `~4.32`; B fill-probability `401.65 / 398.27 / 393.00`, SL `389.00`, TPs `413.23 / 413.23 / 417.50`, aggregate RR `~3.25`.
- tvflow correction: accepted the GPT A/B structure after audit, recalculated margin/leverage and fresh liquidity. A needs about `6x` to fit `1500 USDT` margin; B needs about `5x`. Both remain `RED` live liquidity/orderability, so they are chart-valid `WATCH_ONLY / NOT_LIVE_PLACEABLE` unless Andrea separately gives an explicit RED-liquidity override.
- Corrected report: `reports/deep_analysis/2026-06-10_UNHUSDT_deep_analysis_corrected.md`; corrected Discord reply validates. Feedback record: `reports/deep_analysis_feedback/20260610T211508Z_UNHUSDT_feedback.json`.
- Prompt/README patch: validation is not enough if A/B are present only as headings but effectively suppressed. Future reports must not collapse chart-valid A/B ladders into one conservative leg because liquidity is RED; print full A/B tickets as WATCH_ONLY when needed. TP assignment must be leg-depth aware: shallow legs use nearer resistance; deeper support legs may use broader visible channel/range targets when screenshots support it.

## UNHUSDT Option B 7x live placement
- Andrea asked to place Option B and clarified that future confirmation answers may be simply `confirm`; after the precheck table she replied `confirm with solated 7x`, interpreted as isolated 7x confirmation. Precheck: UNHUSDT regular/normal/profit_loss `0/0/0`, no UNH position; account isolated max available about `3225.29 USDT` before placement; unrelated LLYUSDT long existed.
- Set/verified UNHUSDT isolated margin mode and 7x leverage. The first placement attempt was blocked by local `BITGET_ALLOW_ORDER_PLACEMENT`; Andrea then explicitly said `Enable it! and place the order now!`. Placement was enabled only via per-process environment variables for the order commands, not persistently written to `.env.local`.
- Placed live regular isolated 7x long limit ladder with preset TP/SL under explicit RED liquidity override: B1 orderId `1448717580808908801`, clientOid `oc-unh-optB-7x-20260610-B1`, `1.97 @401.65`, SL `389.00`, TP `413.23`; B2 orderId `1448718129792970753`, clientOid `oc-unh-optB-7x-20260610-B2`, `3.77 @398.27`, SL `389.00`, TP `413.23`; B3 orderId `1448718208020934657`, clientOid `oc-unh-optB-7x-20260610-B3`, `10.00 @393.00`, SL `389.00`, TP `417.50`.
- Postcheck: three UNH regular orders live/unfilled (`baseVolume=0`), isolated `7x`, orderSource `normal`, preset TP/SL attached; no UNH position yet and no separate UNH profit_loss plans. Totals: qty `15.74`, notional `~6222.73`, margin `~888.96 @7x`, planned risk `~99.87`, reward `~324.21`, aggregate RR `~3.25`. RED liquidity override remained: stop-exit GREEN extra `0`, near-depth GREEN, spread RED `~0.1673%`, p10/dead/24h volume RED. TradingView Desktop MCP drew three solid live ladder orders on `BITGET:UNHUSDT.P` 4H. Summary `reports/live_execution/20260610_unh_optionB_7x_live/summary.json`.
