# 2026-05-31

## EWY VOCO trigger / DM failure investigation
- EWY C breakout trigger was valid: cron/state show the 2026-05-30 20:00 UTC 4H candle closed `207.85` above trigger `207.20`, close buffer passed `0.65 >= 0.1996` (`0.10 ATR4H`), close-position passed `0.729 >= 0.60`, current price at alert `207.79` was below max chase `209.60`, and risk/RR gates passed.
- DM did not fail after an attempted send; the EWY job was not configured to DM. Cron `60167d7f-829c-46e4-815e-b69ce42cbe59` used runner `delivery.mode=announce` to `channel:1499631210283008002` and its payload only replied with the watchdog message. Run history shows delivery to the room, with no `messageToolSentTo user:1322306175865323552` record.
- Past successful VOCO DM test `55a9fa08-d558-4851-b645-5f8d1c99d87c` used the direct in-agent Discord message-tool route to `user:1322306175865323552` with runner delivery disabled (`deliveryStatus=not-requested`, `messageToolSentTo user:1322306175865323552`). That is the required critical-alert route.
- Anti-regression: scheduled VOCOs must use `delivery.mode=none`, direct message-tool DM-first routing, primary watchdog run with `-NoStateUpdate`, commit only after notification attempt, and fallback room mention if DM attempts fail. Hardened active AAPL and CLUSDT VOCO cron payloads to this pattern after the EWY investigation.

## EWYUSDT C1 live placement after B cancellation
- Latest EWYUSDT C1 live placement (2026-05-31): Andrea confirmed `Confirm cancel ewyb and place ewy c1 5x override` after C breakout had alerted. Fresh precheck showed the EWY B ladder still live/unfilled (`8.33 @191.20`, `3.43 @196.60`, `1.81 @200.20`), no EWY position, current about `208.71`, and no-chase cap `209.60` still passing. Cancelled all three revised B ladder orders: `1444270249703272449` / `oc-ewy-b-2x-20260529-L3`, `1444270168824508418` / `oc-ewy-b-2x-20260529-L2`, and `1444270122024464385` / `oc-ewy-b-2x-20260529-L1`; post-cancel verification EWY regular/plan `0/0`, no EWY position. Fresh liquidity gate for C1 remained RED (24h quote volume `378630.23` vs required `2037969.25`, dead candles `74.58%`, p10 1m quote volume `0/unavailable`; spread, simulated stop-exit slippage, and depth-to-SL passed). Under explicit RED liquidity override, set/confirmed EWYUSDT isolated margin and `5x` leverage, then placed live C1 limit long order `19.61 @207.85`, SL `202.75`, TP `217.80`, orderId `1444876326824210433`, clientOid `oc-ewy-c1-5x-20260531`. Postcheck: EWY regular orders `1`, plan `0`, C1 status `live`, baseVolume `0`, isolated `5x`, no EWY position yet. Planned risk `100.011`, planned profit `195.1195`, RR `~1.95`, notional `4075.9385`, margin at 5x `815.1877`. Disabled EWY VOCO B+C watchdog cron `60167d7f-829c-46e4-815e-b69ce42cbe59` after placement. Drew TradingView Desktop MCP plan on `BITGET:EWYUSDT.P` 4H successfully (entry/SL/TP entity ids `j9jVFl`/`DMrEnn`/`WAhUeH`). Journal refreshed NoSend; workbook snapshot `reports/trade_journal/bitget_futures_trade_report_2026-05-31T07-08-12-141Z.xls`. Execution summary: `reports/live_execution/20260531_0908_ewy_c1_5x_live/summary.json`.
