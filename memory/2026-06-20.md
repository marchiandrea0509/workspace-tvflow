# 2026-06-20

## Deep-analysis fast-path renderer implementation
- Andrea approved proceeding with the optimization to split final model reasoning from report formatting while preserving format quality.
- Implemented a safe fast path where the model returns content-focused `deep_analysis_decision_v1` JSON and code owns the user-visible report format.
- Builder changes: `scripts/build_deep_analysis_packet_v2.py` now writes `derived/decision_packet_ultra_compact.json` and `llm_decision_request_ultra_compact.md` in addition to existing compact/full artifacts. Existing full/compact workflow remains intact as fallback.
- Added `schemas/deep_analysis_decision.schema.json` and `scripts/render_deep_analysis_from_decision_json.py`. Renderer outputs the required ARM-approved section family, canonical ticket table, split orderability traffic-light tables, risk sizing table, and final verdict, then uses the existing structural validator.
- Documentation patched: `README_DEEP_ANALYSIS_V2.md` and `AGENTS.md` now describe the JSON->renderer fast path and remind that decision JSON is never user-facing.
- Validation evidence: Python compile passed for builder/renderer/finalizer/auditor; no-capture packet build emitted ultra-compact artifacts under `reports/deep_analysis_effort_tests/20260620_203644_INTCUSDT/`; ultra request was ~46.97k chars (~11.7k token estimate) vs compact request ~77.44k chars (~19.4k token estimate), and it avoids the separate ~49.5k-char master prompt in the final pass. Renderer smoke test from `tmp/deep_analysis_decision_sample.json` produced `tmp/deep_analysis_decision_sample_rendered.md`; `validate_deep_analysis_report.py`, `finalize_deep_analysis_delivery.py`, and `audit_deep_analysis_delivery.py` all passed (`AUDIT_PASS`).
- Future use: for a new `Analyse SYMBOL.P`, prefer the fast path when ready: read screenshots, use `llm_decision_request_ultra_compact.md` for final model decision JSON, render with `python scripts\render_deep_analysis_from_decision_json.py --decision <decision.json> --out reports\deep_analysis\<REPORT>.md`, then run finalizer and audit. If JSON/renderer fails, fix it or fall back to the existing full report path; do not send a manual summary.
