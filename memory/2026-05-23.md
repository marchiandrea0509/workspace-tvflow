# 2026-05-23 — tvflow durable memory

## CLUSDT breakdown watchdog
- Andrea uploaded a CLUSDT.P / CLUSDTPERP SHORT-only breakdown watchdog spec for 4H closed-candle confirmation below 94.30, no automatic execution, Discord DM alert only, exact approval phrase `APPROVE CL SHORT`, final recheck before any live order, max planned risk 100 USDT, max leverage 20x, max margin 1500 USDT.
- Added `scripts/clusdt_breakdown_watchdog.js`. It is read-only by default and computes latest fully closed 4H candle, ATR14, 20-period 4H quote-volume average, close distance in ATR, close position in candle range, current overextension, existing CLUSDT positions/orders, RWA-aware liquidity gate, and a proposed TP1/TP2 market-after-approval ticket sized from live estimated entry to SL 97.25.
- State/lock path: `reports/watchdogs/clusdt_breakdown_watchdog_state.json`. Dry-run output path used: `reports/watchdogs/clusdt_breakdown_watchdog_dryrun_latest.json`. The script has modes `main`, `validity`, and `final`; `final` is for later exact-approval handling and still only performs final recheck / ticket output, not placement.
- Created cron `CLUSDT.P Breakdown Watchdog 4H Main` job id `14aceb4d-6549-4983-8d84-0bba330e0c06`, schedule `5 0,4,8,12,16,20 * * *` UTC. It runs the main script and DMs Andrea only if status READY or if a blocker/error occurs; otherwise NO_REPLY.
- Created cron `CLUSDT.P Breakdown Watchdog Ticket Validity` job id `a3c7ad62-aeac-4186-b88a-e0b071fc06f2`, schedule every 15 minutes. It stays quiet unless an active ticket expires or invalidates.
- Both cron payloads explicitly forbid live placement/cancel/modify commands. If Andrea later replies exactly `APPROVE CL SHORT`, run the script in `--mode final` first, then only place live orders if final recheck passes and execution is explicitly confirmed/within the approved wording.
- Validation: `node --check scripts/clusdt_breakdown_watchdog.js` passed. Dry run on 2026-05-23 showed NO_SIGNAL for the 12:00 UTC closed 4H candle: close 96.707 not below 94.30, close distance -1.106 ATR, close position 47.4%, and liquidity gate RED for the hypothetical current-price market ticket.

## RWA liquidity-gate warm-up patch
- Andrea noted Bitget tokenized stock/RWA futures are always technically open, but liquidity follows real stock-market rhythm; using full 60/120m 1m-candle windows shortly after active liquidity returns can falsely fail because stale low-volume candles remain in the sample.
- Patched `bitget-futures-harness/lib/liquidityGate.js` so `isRwa=YES` contracts can use `rwa_active_session_warmup` for `deadCandles` and `volumeStress` instead of blindly hard-failing stale p10 candles.
- Warm-up logic detects a recent inactive-to-active transition using 1m quote-volume candles; if Bitget never shows a clean boundary, it can use a recent 30-candle RWA override, but that fallback is YELLOW at best and still requires explicit confirmation before live placement.
- Gate report now includes active-candle/window count, active median 1m quote volume, position/median ratio, and effective mode. Normal non-RWA symbols keep normal 60/120m p10 logic.
- Validation: `node --check bitget-futures-harness/lib/liquidityGate.js` passed. CLUSDT swing-short gate check after patch returned YELLOW, not live-placed: spread/24h/orderbook stop slippage/depth GREEN; normal p10 still BLOCK; RWA warmup `27/30` active candles, median 1m quote volume `~15076.53 USDT`, position/median ratio `~16.59%`, fallback confirmation-gated.
# 2026-05-23 — tvflow durable memory

## RWA liquidity-gate warm-up patch
- Andrea noted Bitget tokenized stock/RWA futures are always technically open, but liquidity follows real stock-market rhythm; using full 60/120m 1m-candle windows shortly after active liquidity returns can falsely fail because stale low-volume candles remain in the sample.
- Patched `bitget-futures-harness/lib/liquidityGate.js` so `isRwa=YES` contracts can use `rwa_active_session_warmup` for `deadCandles` and `volumeStress` instead of blindly hard-failing stale p10 candles.
- Warm-up logic detects a recent inactive-to-active transition using 1m quote-volume candles; if Bitget never shows a clean boundary, it can use a recent 30-candle RWA override, but that fallback is YELLOW at best and still requires explicit confirmation before live placement.
- Gate report now includes active-candle/window count, active median 1m quote volume, position/median ratio, and effective mode. Normal non-RWA symbols keep normal 60/120m p10 logic.
- Validation: `node --check bitget-futures-harness/lib/liquidityGate.js` passed. CLUSDT swing-short gate check after patch returned YELLOW, not live-placed: spread/24h/orderbook stop slippage/depth GREEN; normal p10 still BLOCK; RWA warmup `27/30` active candles, median 1m quote volume `~15076.53 USDT`, position/median ratio `~16.59%`, fallback confirmation-gated.

## JD live-order simulation and slippage-sizing default
- Andrea asked to simulate a JDUSDT.P live long ladder: L1/L2/L3 `30.30/30.20/30.10`, SL `29.80`, TP1/TP2/TP3 `31.30/31.70/32.00`. Initial real-style 100 USDT risk ticket was corrected after Andrea pointed out missing liquidity/slippage analysis.
- JD gate finding: spread was OK around `30.48/30.49`, but 100 USDT risk implied ~250 JD / ~7.55k USDT notional, too large versus thin JD liquidity and with estimated stop-market extra loss around `~10.25 USDT`; verdict should be NO-GO unless downsized.
- Durable live-order fallback rule agreed with Andrea: when a live Bitget order request fails liquidity/slippage gate, immediately provide two downsized size proposals rather than only rejecting: (1) cap extra stop-market slippage cost to `slippage_pct × original/base planned risk` (default currently 5% of 100 USDT = 5 USDT), and (2) cap extra stop-market slippage cost to `slippage_pct × new planned no-slippage risk`. Keep `slippage_pct` parameterized.
- For each fallback proposal, solve size from the live stop-exit orderbook depth, round down to symbol precision, and show qty per entry/total qty, planned no-slippage risk, estimated extra slippage, total risk after slippage, notional/margin, and remaining volume/depth pass/caution/no-go status.
- JD example results from the discussion: original-risk 5 USDT slippage cap yielded about `58.32 JD` per entry / `174.96 JD` total / planned risk `~69.98 USDT`; self-referenced 5% new-risk cap yielded about `47.88 JD` per entry / `143.64 JD` total / planned no-slippage risk `~57.456 USDT`, estimated extra slippage exactly `~5%` of that planned risk.

## Trading alert watchdog DM test status
- Andrea asked to set up/test a watchdog for trading alerts that sends Discord DMs. Created disabled template config at `watchdog/trading_alert_watchdog.json` with target intended for Andrea (`1322306175865323552`) and example disabled price-level rule.
- DM delivery investigation: OpenClaw/Discord cron/direct outbound attempts to `user:1322306175865323552` resolved incorrectly as a channel and failed with `Unknown Channel`; attempts to `dm:1322306175865323552` resolved as DM but still failed with generic Discord/OpenClaw delivery `Error`.
- Andrea confirmed manually that a DM thread with bot `Gray#4156` exists and works: user sent `Test DM`/`Test`, Gray replied `DM received ✅` and `Still here ✅`. Therefore the remaining failure is not Discord privacy/blocking; it is likely OpenClaw outbound-DM target/plugin behavior for agent/cron delivery.
- Practical workaround recommended: create/use a private Discord channel such as `#trade-alerts-private` visible only to Andrea and Gray, and target the trading watchdog there instead of Discord DM until OpenClaw DM outbound routing is fixed.

- Correction/update after DM test: Andrea later confirmed receiving the Discord DM text ?? Trading watchdog DM direct-tool test: alert route works. No trading action taken. Therefore Discord DM delivery can work via the direct-tool path after the manual Gray DM thread existed. Prior cron delivery/fallback queue attempts still failed, so future watchdog implementation should prefer the proven direct-tool send path for alerts, not cron fallback delivery, unless cron direct messaging is separately validated.
