# 2026-05-29

## Discord file / ZIP delivery anti-regression
- Andrea reported that a final assistant `MEDIA:<zip>` line again failed to attach a ZIP in Discord when exporting `exports/virtual_oco_watchdog_rules_script.zip`.
- Root durable lesson: for Discord file/ZIP exports in this workspace, do **not** use final-chat `MEDIA:<path>` as the primary delivery path. It is unreliable for Discord ZIP/output attachments.
- Use the proven direct media-send workflow first: `openclaw message send --channel discord --target 1487602980093165658 --reply-to <discord_message_id> --message "File attached: <summary>." --media <absolute_file_path> --json --verbose`.
- Verify success before claiming attachment delivery: command should return success JSON / Discord `messageId`; if it hangs/fails, treat delivery as blocked and report the blocker instead of saying attached.
- Updated `TOOLS.md` with a general Discord file/ZIP delivery rule, not just Strategy Test exports. Historical proof remains `memory/2026-04-30.md`: direct send worked after `MEDIA:` failed, returning Discord messageId `1499525125626663062` in channel `1487602980093165658`.

## VOCO breakout candle-quality gate from Andrea ZIP
- Andrea uploaded a modified VOCO package ZIP and asked to apply the README/rule changes to the package and the running AAPL watchdog.
- Applied the uploaded rule: breakout/breakdown family C now has objective candle-quality gates in `scripts/virtual_oco_watchdog.js`.
- New defaults/config keys: `breakoutMinCloseBufferAtr` / family `minCloseBufferAtr` default `0.10`; `breakoutMinClosePositionLong` / family `minClosePositionLong` default `0.60`; `breakoutMaxClosePositionShort` / family `maxClosePositionShort` default `0.40`.
- Long C requires `close - trigger >= minCloseBufferAtr * ATR4H` and `closePosition=(close-low)/(high-low) >= minClosePositionLong`; short C requires `trigger - close >= minCloseBufferAtr * ATR4H` and `closePosition <= maxClosePositionShort`.
- Updated `README_VIRTUAL_OCO_WATCHDOG.md`, `watchdog/virtual_oco_watchdog.template.json`, and running AAPL config `watchdog/virtual_oco_AAPLUSDT_20260528_C.json` with these gates.
- Re-ran the AAPL watchdog commit check with `-Force` after patching. State remains `ARMED`; latest checked 4H candle `2026-05-29T00:00:00Z`, close `311.60` vs trigger `313.64`; new gates are present in state and fail as expected (`breakout close buffer -2.04 >= 0.1835`, close position `0.585 >= 0.6`). No live order was placed.
- Validation passed: `node --check scripts/virtual_oco_watchdog.js`; JSON parse for template and AAPL config; AAPL watchdog check returned `CHECKED` / no trigger.
