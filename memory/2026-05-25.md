# 2026-05-25 — tvflow durable memory

## Durable learning / anti-regression loop
- Andrea confirmed `PROJECT_STATE.md` should be used as an operational continuity guardrail so tvflow does not repeat fixed weaknesses or blindly use modified tools.
- Added an explicit durable learning loop to `AGENTS.md`: meaningful workflow/prompt/live-order changes should be preserved in the smallest useful set of `PROJECT_STATE.md`, relevant README/schema/prompt files, and dated memory.
- Future durable notes should capture: what changed, why it changed, the discovered mistake/API quirk, what future runs must do differently, and validation/postcheck evidence.
- For live Bitget workflows, fixed weaknesses should be encoded in code or docs, not only remembered from chat.

## CLUSDT TP trailing split hardening
- Added/used `bitget-futures-harness/scripts/set-tp-trailing-split.js` to convert existing CLUSDT.P fixed TP rows into fixed TP + trailing TP rows.
- Live API quirk discovered: Bitget lists TP/SL rows through pending `planType=profit_loss`, but targeted cancellation of fixed TP rows requires `cancel-plan-order` with `planType: "profit_plan"` and `orderIdList`; top-level `orderId` is unsafe/insufficient for this case.
- Updated `bitget-futures-harness/README.md` with the anti-regression workflow and the TP trailing split / plan-order cancellation rule.
- Final CLUSDT postcheck after repair: fixed TPs S1 `3.98 @ 88.98`, S2 `6 @ 88.70`, S3 `7.41 @ 83.05`; trailing `track_plan` rows S1 `1.70 @ 88.98 callbackRatio 1.1`, S2 `4 @ 88.70 callbackRatio 1.3`, S3 `7.40 @ 83.05 callbackRatio 1.6`; old full-size TPs canceled; original SLs unchanged.

## Deep-analysis feedback loop v0
- Andrea asked to start a simple continuous-improvement structure for weak deep analysis, including comparison against GPT tickets and optional trade-journal outcome feedback.
- Implemented v0 feedback artifacts: `README_DEEP_ANALYSIS_FEEDBACK.md`, `schemas/deep_analysis_feedback.schema.json`, and `scripts/create_deep_analysis_feedback.py`.
- Feedback records live under `reports/deep_analysis_feedback/*.json` and capture tvflow ticket, GPT ticket/source text, Andrea/user-preferred ticket, issue tags, scorecard, actionable lesson, improvement actions, and optional journal outcome.
- Journal outcome linkage is intentionally optional so it does not overcomplicate live execution: a feedback record can start as comparison-only, then later be updated after `run_bitget_journal_update.ps1` with entry/SL/TP/ladder/liquidity outcome lessons.
- Rule: GPT is an audit/teacher source, not execution authority. tvflow should compare, identify the real weakness, and patch prompt/tool/docs/memory only when the issue is repeated, clear, or safety-critical.
