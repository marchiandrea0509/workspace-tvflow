# 2026-06-13

## VOCO cron cleanup and terminal deletion hardening
- Andrea reported stale/unused old VOCO cron jobs cluttering the OpenClaw dashboard and requested cleanup plus a tool change so expired/triggered VOCO cron jobs are deleted completely.
- Removed stale disabled VOCO/trading-watchdog cron jobs from the scheduler: GEUSDT VOCO A+C1, EWYUSDT VOCO B+C, COPPERUSDT VOCO B/C, AAPLUSDT Case C VOCO, old Trading Watchdog DM tests/template, and the disabled CLUSDT ticket-validity watchdog. Kept unrelated workspace/screener/MT5/llama/context jobs untouched.
- Patched `scripts/virtual_oco_watchdog.js`: terminal VOCO statuses (`ALERTED`, `LIVE_LEG_FILLED`, `EXPIRED`, `INVALIDATED`) now call `openclaw cron remove <cronId>` by default, with `delete` as fallback, instead of `openclaw cron disable <cronId>`.
- Future VOCO configs should use `scheduler.deleteCronAfterTerminal: true`; legacy `cancelCronAfterTerminal`, `removeCronAfterTerminal`, and `disableCronAfterTerminal` are accepted aliases but now mean remove/delete rather than leave disabled.
- Updated `README_VIRTUAL_OCO_WATCHDOG.md`, `watchdog/virtual_oco_watchdog.template.json`, existing `watchdog/virtual_oco_*.json` configs, and GE/CL VOCO cron prompts to say remove/delete cron after terminal state.
