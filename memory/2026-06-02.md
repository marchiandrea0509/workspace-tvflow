
## TradingView Desktop placement-arrow price correction
- Andrea clarified the white arrow semantics: for live order drawings, the arrow should mark the current/market price at the moment of order placement, not the limit entry level. The BTC static B drawing had incorrectly put the single white arrow at the S1 entry `69500`.
- Patched `C:\Users\anmar\tools\tv_draw_trade_plan.ps1` so arrows can use `arrow_price`, `arrowPrice`, `current_price`, `currentPrice`, `placement_price`, `placementPrice`, `market_price`, `marketPrice`, `last_price`, or `lastPrice` from the order or top-level plan. It falls back to entry only for backward compatibility. Parser validation passed.
- Updated `C:\Users\anmar\.openclaw\skills\tv-draw-trade-plan\SKILL.md`: the white arrow is a placement/current-price marker, not an entry marker; future live ladder plans must include explicit `arrow_price` / `current_price` / `placement_price` from the placement moment.
- Also fixed `C:\Users\anmar\tools\tradingview-mcp\src\core\drawing.js` list/get/remove/clear helpers to use resolved `evaluate/getChartApi`, which made `draw remove/list` work again. Validation: `node --check` passed.
- Removed the wrong BTC chart arrow entity `jpfP7B` and drew the corrected white arrow at S1 placement time `1780433241` and market price `~67323`; new arrow entity `k68aBk`. BTC live order lines were not changed.

## VOCO watchdog hybrid/C100 patch refresh
- Andrea supplied a VOCO watchdog patch and asked to treat it as additive, preserving existing good work. Current watchdog already had most of the May hybrid-mode implementation, but this pass closed remaining gaps and aligned prompt/docs.
- Patched `scripts/virtual_oco_watchdog.js`: risk-verification failure now uses the requested wording; read-only live-state inference now checks `normal_plan` in addition to `profit_loss` and `track_plan`; ladder status is normalized to `NONE`, `OPEN_UNFILLED`, `PARTIALLY_FILLED`, `FULLY_FILLED`, `UNKNOWN`; C100 status is normalized to `INACTIVE`, `ACTIVE`, `UNKNOWN`; alert metadata now prints required human-readable fields (`Execution mode`, `Execution mode source`, `Risk cap USD`, `Ladder status`, `C100 status`, `Risk mode`); final action line explicitly says the watchdog did not place/cancel/modify live orders.
- Patched `watchdog/virtual_oco_watchdog.template.json` with default `liveState.planTypes = [normal_plan, profit_loss, track_plan]`.
- Patched `README_VIRTUAL_OCO_WATCHDOG.md` with required fields, status enums, C100 compliance output, and normal-plan inference note.
- Patched deep-analysis prompt/docs (`prompts/master_trade_analysis_prompt_v2.md`, `scripts/build_deep_analysis_packet_v2.py`, `README_DEEP_ANALYSIS_V2.md`, `schemas/deep_analysis_packet_contract_v2.md`) so D/VOCO now distinguishes `PURE_VOCO`, `HYBRID_VOCO`, and `HYBRID_C100` while preserving `COMBO_100` analysis language. The VOCO watchdog remains alert-only; trigger/candle/expiry/ticket logic was not changed.
- Validation passed: `node --check scripts/virtual_oco_watchdog.js`; `python -m py_compile scripts/build_deep_analysis_packet_v2.py scripts/validate_deep_analysis_report.py`; JSON parse for `watchdog/virtual_oco_watchdog.template.json`; disabled-template smoke run returned `DISABLED`/`NO_REPLY`.

