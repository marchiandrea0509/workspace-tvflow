# 2026-06-21

## Read-only Bitget position/order diagnostic tool
- Andrea requested a diagnostic tool to inspect active open orders/positions and recommend KEEP / REDUCE_RISK / TP_REFRESH / CANCEL_UNFILLED / FULL_REFRESH / EXIT_OR_INVALID without automatically changing exchange state.
- Reviewed the proposal and implemented it with one critical safety boundary: the tool is advisory/read-only only; any cancel, TP/SL edit, partial close, exit, or new order remains a separate explicitly confirmed live-execution workflow.
- Added `bitget-futures-harness/scripts/position-order-diagnostic.js`, npm script `position-diagnostic`, and `bitget-futures-harness/README_POSITION_DIAGNOSTIC.md`; linked it from the harness README.
- The tool reads all pending regular futures orders, open futures positions, `normal_plan`/`profit_loss`/`track_plan` rows, ticker, 1H/4H/1D candles, local structured execution summaries where available, and the existing liquidity gate. It outputs JSON and markdown reports under `bitget-futures-harness/reports/diagnostics/`.
- Validation: `node --check scripts/position-order-diagnostic.js`; package/report JSON parse passed; live read-only run saved `reports/diagnostics/20260621_live_active_diagnostic.md` and `.json`.
- Live read-only test snapshot: 11 regular open orders, 1 open position, 4 pending TP/SL plan rows. Verdicts: ARM TP_REFRESH, ASML FULL_REFRESH, GE TP_REFRESH, INTC TP_REFRESH, UNH FULL_REFRESH. No exchange orders were placed/canceled/modified.

## ASML diagnostic discrepancy / pullback TP bug fix
- Andrea compared the ASML refresh result with GPT, which said not to change ASML. Rechecked live Bitget state: actual ASML orders remain unfilled `0.85 @1890.98 SL1844 TP1975` and `2.66 @1866.52 SL1844 TP1943.5`, with no ASML position.
- Root discrepancy: GPT assumed/evaluated the intended GPT Option B (`1908/1874 SL1855`), while the exchange still has the earlier tvflow TP-mod variant (`1890.98/1866.52 SL1844`). More importantly, my diagnostic logic incorrectly treated `current price > TP` as stale for an unfilled pullback buy limit. That is wrong when `entry < TP < current`: if price first pulls back to entry, TP can still be a valid bounce target.
- Patched `bitget-futures-harness/scripts/position-order-diagnostic.js`: a deeper unfilled pullback order is no longer marked stale solely because current price is above TP, as long as TP is on the profitable side of entry. Added README note.
- Validation: `node --check scripts/position-order-diagnostic.js`; reran ASML read-only diagnostic to `bitget-futures-harness/reports/diagnostics/20260621_asml_discrepancy_recheck.md/.json`. Corrected ASML diagnostic verdict is `KEEP_BUT_MONITOR`, with both live ASML orders mechanically `KEEP_UNCHANGED`; liquidity remains RED, no exchange changes made.

## ASML deep-distance diagnostic refinement
- Andrea correctly noted that if price pushes too far away above a resting pullback entry, the original thesis may decay and deserve modification. Refined the diagnostic distinction: current price above TP is not stale by itself for an unfilled buy limit, but large distance from current price should not be `KEEP_UNCHANGED` either.
- Added `deepPullbackMonitorAtr` option to `position-order-diagnostic.js` (default `4.0 ATR4H`). Unfilled pullback orders farther than this are classified `KEEP_BUT_MONITOR` / deep contingency, not active unchanged. Distance alone still does not trigger cancel/refresh; modification depends on stale horizon, new higher base, loss of original structure, or explicit user decision.
- Reran ASML read-only diagnostic to `bitget-futures-harness/reports/diagnostics/20260621_asml_distance_threshold_recheck.md/.json`: CP `1957.18`, ATR4H `13.9957`; ASML B1 `1890.98` is `4.73 ATR4H` away, B2 `1866.52` is `6.48 ATR4H` away. Corrected verdict: `KEEP_BUT_MONITOR`; treat both as lower-fill-probability deep contingencies, not immediate upward refresh/cancel.

## UNH diagnostic discrepancy / partially-filled static campaign fix
- Andrea compared the UNH diagnostic with GPT. Fresh read-only Bitget state: UNH L1/L2 are filled into one long position (`total 5.74`, avg `399.4300`, mark `398.01`, unrealized about `-8.15 USDT`), TP/SL plan rows active for filled sizes (`413.23` TP and `389` SL), and L3 remains live/unfilled (`10 @393`, SL `389`, TP `417.5`).
- GPT's key point is correct: this is a partially-filled static ticket. The remaining L3 is part of the original full-risk plan, not a new stale standalone order. My diagnostic over-weighted ticket age `>5d` and TP distance, causing an overly strong `FULL_REFRESH` verdict.
- Patched `position-order-diagnostic.js`: if an open same-side position exists, a remaining same-side unfilled leg beyond the 5-day horizon is classified as `KEEP_BUT_MONITOR` / optional risk-reduction, not automatic `FULL_REFRESH`. Position TP distance now becomes monitor/reclaim-warning rather than automatic `TP_REFRESH` when risk/SL/TP are otherwise coherent. Fixed action wording for `KEEP_BUT_MONITOR` positions.
- Validation: `node --check scripts/position-order-diagnostic.js`; reran UNH read-only diagnostic to `bitget-futures-harness/reports/diagnostics/20260621_unh_gpt_comparison_corrected.md/.json`. Corrected verdict: `KEEP_BUT_MONITOR`; keep current static plan, monitor reclaim/weakness triggers, optional L3 cancel only if Andrea explicitly chooses to reduce risk. No exchange changes made.

## Diagnostic tool patch verification after Andrea request
- Andrea asked to patch the diagnostic tool after ASML/UNH GPT comparisons. Verified the current patched `bitget-futures-harness/scripts/position-order-diagnostic.js` with `node --check` and reran read-only regression diagnostics for ASML and UNH.
- ASML verification output saved to `bitget-futures-harness/reports/diagnostics/20260621_asml_patch_verify.md/.json`: corrected verdict `KEEP_BUT_MONITOR`; deep pullback orders are no longer false `FULL_REFRESH`, but are flagged as lower-fill-probability contingencies when >4 ATR4H away.
- UNH verification output saved to `bitget-futures-harness/reports/diagnostics/20260621_unh_patch_verify.md/.json`: corrected verdict `KEEP_BUT_MONITOR`; partially-filled static campaign is not auto-refreshed solely due to age/TP distance; optional risk reduction still requires explicit confirmation.
- `git diff --check` passed for the diagnostic script and README. No exchange changes were made.
