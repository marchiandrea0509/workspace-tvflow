# 2026-06-27

## HOODUSDT GPT Option A 6x live placement
- Andrea compared tvflow HOOD analysis with GPT; GPT Option A was accepted as the better chart plan: buy limits `94.60` and `92.20`, SL `89.30`, TPs `106.08` / `103.63`, planned risk `~100 USDT`, aggregate RR `~3.23`.
- Initial confirmed live request: `CONFIRM PLACE HOOD GPT OPTION A 3X ISOLATED LIVE WITH RED LIQUIDITY OVERRIDE`. 3x placement failed before any order because Bitget returned `40762 order amount exceeds the balance`; postcheck then showed no HOOD orders/position.
- Andrea then asked to check spot balance, move available spot to futures if present, and adapt leverage to fit the order. Spot USDT available `421.237078074`; transferred `421.23 USDT` spot -> usdt_futures, transferId `1454826246056869888`, clientOid `oc-hood-xfer-20260627`.
- Futures usable margin after transfer was `~458.679 USDT`; selected isolated `6x` because full ladder notional `~2622 USDT` needs margin `~437 USDT`, below original max margin `1000 USDT` and fitting the available margin. Set/confirmed HOOD isolated `6x` long leverage.
- Final just-in-time liquidity gate stayed RED: stop-exit GREEN (`0 USDT` extra / `0%` planned risk), near-depth GREEN (`0.25% ~113,304 USDT = 43.21x`, `0.50% ~146,097 USDT = 55.72x`), spread GREEN (`~0.0405%`), p10 weak-minute volume RED (`~20377%` of p10 non-dead 1m quote volume `~12.87 USDT`), dead candles RED (`~83.33%`), 24h volume YELLOW (`~1.245M USDT = 474.79x notional`). Explicit RED override and leverage-adaptation instruction used.
- Placed live regular limit longs with preset TP/SL: A1 orderId `1454826558517309441`, clientOid `oc-hood-gptA-6x-20260627-A1`, `7.55 @94.60`, SL `89.30`, TP `106.08`; A2 orderId `1454826577349734401`, clientOid `oc-hood-gptA-6x-20260627-A2`, `20.69 @92.20`, SL `89.30`, TP `103.63`.
- Postcheck: two HOOD regular orders live/unfilled (`baseVolume=0`), isolated `6x`, preset TP/SL attached, no HOOD position yet, normal/profit_loss/track rows `0/0/0`. Account usable margin after placement `~15.7537 USDT`. Post-fill monitors PIDs `18644` and `17896`, prefix `oc-hood-gptA-6x-20260627`; TradingView Desktop MCP drew two solid live orders with one white placement arrow around `98.79`. Summary `reports/live_execution/20260627_hood_gptA_6x_live/summary.md`.
