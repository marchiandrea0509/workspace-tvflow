# 2026-06-28

## GEUSDT HYBRID_VOCO A+C1 alert-only watchdog armed
- Andrea asked to place a Virtual order / VOCO on GE C1 while keeping the current GE ladder live. This was implemented as alert-only `HYBRID_VOCO`, not live exchange execution.
- Existing GE Option A live ladder remained untouched: A1 `4.84 @356.05` SL `347.80` TP `368.89`, A2 `12.76 @352.50` SL `347.80` TP `371.43`, isolated `8x`, total planned risk `~99.90`, reward `~303.70`, RR `~3.04`.
- Created config `watchdog/virtual_oco_GEUSDT_20260628_C1.json`, state `reports/watchdogs/virtual_oco/VOCO_GEUSDT_20260628_A_C1.state.json`, prompt `prompts/cron_virtual_oco_GEUSDT_20260628_C1.md`.
- C1 is virtual/alert-only: trigger requires closed 4H above `379.38`, low holding at/above `376.00`, breakout close quality, no-chase max current `<=381.75`, ticket reference `11.36 @380.20`, SL `371.40`, TP `394.50`, risk `~99.97`, RR `~1.63`, margin `~539.88 @8x`. If it alerts, any live placement still requires fresh liquidity/orderability and a separate explicit confirmation.
- Dry-run validation passed with `NO_TRIGGER`; live ladder detected as `OPEN_UNFILLED`, duplicate pullback alert suppressed, C1 waiting because latest closed 4H close was `366.51` below `379.38`. Cron `a6f23060-93d5-4c7d-ba88-f3e29cb8a877` named `GEUSDT HYBRID_VOCO A+C1 Watchdog 4H` enabled at `5 0,4,8,12,16,20 * * * UTC`, delivery to the current GE Discord thread, and removes itself after terminal state.

## Deep-analysis ticket max-margin and liquidation sanity patch
- Andrea requested changing generated trade-ticket max margin from `1500 USDT` to `500 USDT` and making liquidation-price-vs-SL sanity always performed.
- Patched active deep-analysis ticket generation defaults and contract text: `scripts/build_deep_analysis_packet_v2.py` default `--max-margin-usdt 500`, wrapper default `scripts/run_deep_analysis_packet_v2.ps1 -MaxMarginUsdt 500`, prompt/docs/schema/memory updated to `500 USDT max margin` semantics.
- Added/strengthened mandatory selected-leverage liquidation-vs-SL safety exposure: deep-analysis packets now carry `selected_liquidation_safety_check`, ultra-compact decision packets preserve `estimated_liquidation`, `liquidation_vs_sl_gap_atr`, `liquidation_beyond_sl`, and `passes`, and the final report validator now requires a liquidation/liq-vs-SL row in Orderability/Risk feasibility.
- Updated VOCO/watchdog defaults for future tickets: `scripts/virtual_oco_watchdog.js` fallback max margin `500`, `watchdog/virtual_oco_watchdog.template.json` `maxMarginUsdt=500`; `scripts/clusdt_breakdown_watchdog.js` now sizes to `marginMax=500`, gates `Margin <= 500`, and estimates/prints short liquidation safety vs SL with an ATR buffer.
- Existing live orders and historical reports/configs were not modified; pre-existing armed configs that explicitly contain `maxMarginUsdt: 1500` remain as historical/previously-approved configs unless Andrea separately asks to migrate them.
