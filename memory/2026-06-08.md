# 2026-06-08

## CLUSDT live SL widened after margin add
- Andrea asked to move the live `CLUSDT.P` / Bitget `CLUSDT` short SL to previous-week high plus standard ATR buffer; calculated target SL was `97.37` from prior-week high `96.9` plus `0.35 x ATR4H`.
- Fresh precheck showed live CLUSDT short `37.5 @92.683333333332`, isolated `20x`, margin `173.78125`, liquidation `96.775556881462`, and two live `loss_plan` SL rows totaling `37.5` at `95.35` (`25` and `12.5`). Because target SL `97.37` was above liquidation, refused SL-only change and requested explicit margin-first confirmation.
- Andrea confirmed `CONFIRM CLUSDT ADD ISOLATED MARGIN THEN MOVE SL TO 97.37`. Added `50 USDT` isolated margin to the CLUSDT short via Bitget `/api/v2/mix/account/set-margin` with `holdSide=short`, then modified the two existing `profit_loss` loss-plan rows in place via `/api/v2/mix/order/modify-tpsl-order` to trigger `97.37`, market execute (`executePrice=0`), sizes `25` and `12.5`.
- Postcheck verified CLUSDT short unchanged in size/entry, margin now `223.78125`, liquidation now `98.101465128611`, and both SL loss plans live at `97.37`; TP profit plans unchanged at `87.92` size `25` and `88.98` size `12.5`. Futures available decreased by `50 USDT` to `2865.42900063`.
- Current gross planned risk from entry to SL is approximately `(97.37 - 92.683333333332) * 37.5 = 175.75 USDT`, before fees/slippage. This was a confirmed live exchange write; no regular orders were opened/cancelled, only isolated margin add and TP/SL plan modification.
