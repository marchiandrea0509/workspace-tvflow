# 2026-06-08

## CLUSDT live SL widened after margin add
- Andrea asked to move the live `CLUSDT.P` / Bitget `CLUSDT` short SL to previous-week high plus standard ATR buffer; calculated target SL was `97.37` from prior-week high `96.9` plus `0.35 x ATR4H`.
- Fresh precheck showed live CLUSDT short `37.5 @92.683333333332`, isolated `20x`, margin `173.78125`, liquidation `96.775556881462`, and two live `loss_plan` SL rows totaling `37.5` at `95.35` (`25` and `12.5`). Because target SL `97.37` was above liquidation, refused SL-only change and requested explicit margin-first confirmation.
- Andrea confirmed `CONFIRM CLUSDT ADD ISOLATED MARGIN THEN MOVE SL TO 97.37`. Added `50 USDT` isolated margin to the CLUSDT short via Bitget `/api/v2/mix/account/set-margin` with `holdSide=short`, then modified the two existing `profit_loss` loss-plan rows in place via `/api/v2/mix/order/modify-tpsl-order` to trigger `97.37`, market execute (`executePrice=0`), sizes `25` and `12.5`.
- Postcheck verified CLUSDT short unchanged in size/entry, margin now `223.78125`, liquidation now `98.101465128611`, and both SL loss plans live at `97.37`; TP profit plans unchanged at `87.92` size `25` and `88.98` size `12.5`. Futures available decreased by `50 USDT` to `2865.42900063`.
- Current gross planned risk from entry to SL is approximately `(97.37 - 92.683333333332) * 37.5 = 175.75 USDT`, before fees/slippage. This was a confirmed live exchange write; no regular orders were opened/cancelled, only isolated margin add and TP/SL plan modification.

## LLYUSDT Bitget buyLimitPriceRatio interpretation correction
- Andrea manually placed and then canceled a standard regular LLYUSDT buy limit at `1135` (`0.94`, TP `1200`, SL `1092.62`) from Android; Bitget history confirmed it as `orderSource=normal`, `orderType=limit`, `status=canceled`, not a trigger/plan order.
- tvflow had incorrectly treated `buyLimitPriceRatio=0.02` as a lower bound blocking passive buy limits more than 2% below mark. This was wrong. For passive buy limits below market, Bitget accepted the order; the ratio should not be used as a hard lower-band blocker for standard pullback buy limits.
- Future live prechecks must not block passive buy pullback limits solely because `price < mark * (1 - buyLimitPriceRatio)`. Validate by actual Bitget order semantics/history/docs or, when uncertain, explain uncertainty instead of declaring a hard blocker. Continue to check liquidity, duplicates, margin/leverage/liquidation, and exact confirmation boundaries.
# 2026-06-08

## CLUSDT live SL widened after margin add
- Andrea asked to move the live `CLUSDT.P` / Bitget `CLUSDT` short SL to previous-week high plus standard ATR buffer; calculated target SL was `97.37` from prior-week high `96.9` plus `0.35 x ATR4H`.
- Fresh precheck showed live CLUSDT short `37.5 @92.683333333332`, isolated `20x`, margin `173.78125`, liquidation `96.775556881462`, and two live `loss_plan` SL rows totaling `37.5` at `95.35` (`25` and `12.5`). Because target SL `97.37` was above liquidation, refused SL-only change and requested explicit margin-first confirmation.
- Andrea confirmed `CONFIRM CLUSDT ADD ISOLATED MARGIN THEN MOVE SL TO 97.37`. Added `50 USDT` isolated margin to the CLUSDT short via Bitget `/api/v2/mix/account/set-margin` with `holdSide=short`, then modified the two existing `profit_loss` loss-plan rows in place via `/api/v2/mix/order/modify-tpsl-order` to trigger `97.37`, market execute (`executePrice=0`), sizes `25` and `12.5`.
- Postcheck verified CLUSDT short unchanged in size/entry, margin now `223.78125`, liquidation now `98.101465128611`, and both SL loss plans live at `97.37`; TP profit plans unchanged at `87.92` size `25` and `88.98` size `12.5`. Futures available decreased by `50 USDT` to `2865.42900063`.
- Current gross planned risk from entry to SL is approximately `(97.37 - 92.683333333332) * 37.5 = 175.75 USDT`, before fees/slippage. This was a confirmed live exchange write; no regular orders were opened/cancelled, only isolated margin add and TP/SL plan modification.

## LLYUSDT Bitget buyLimitPriceRatio interpretation correction
- Andrea manually placed and then canceled a standard regular LLYUSDT buy limit at `1135` (`0.94`, TP `1200`, SL `1092.62`) from Android; Bitget history confirmed it as `orderSource=normal`, `orderType=limit`, `status=canceled`, not a trigger/plan order.
- tvflow had incorrectly treated `buyLimitPriceRatio=0.02` as a lower bound blocking passive buy limits more than 2% below mark. This was wrong. For passive buy limits below market, Bitget accepted the order; the ratio should not be used as a hard lower-band blocker for standard pullback buy limits.
- Future live prechecks must not block passive buy pullback limits solely because `price < mark * (1 - buyLimitPriceRatio)`. Validate by actual Bitget order semantics/history/docs or, when uncertain, explain uncertainty instead of declaring a hard blocker. Continue to check liquidity, duplicates, margin/leverage/liquidation, and exact confirmation boundaries.

## LLYUSDT deep analysis and GPT Option B prechecks
- Andrea asked for deep analysis on `LLYUSDT`; report `reports/deep_analysis/2026-06-08_LLYUSDT_deep_analysis.md` was created and passed validator/render gate. tvflow's own report was correctly formatted but judged levels too deep/conservative by Andrea; he preferred GPT's option logic.
- tvflow initial LLY conclusion: no live LLY position/orders/plans; bullish but post-spike; original shallow/deep ticket audit failed R:R and RWA liquidity was RED. Key levels included 1240 spike high, 1165/1170 resistance, 1138/1133 value, 1121/1106 broad pullback, and 1101 major support.
- GPT revised Option B that Andrea preferred: L1 `1135.00`, L2 `1124.69`, shared SL `1092.62`, TP1 `1200.00`, TP2 `1240.00`, risk split approximately 40/60. Rounded Bitget quantities checked by tvflow: L1 `0.94`, L2 `1.87`, total qty `2.81`, total notional `~3170.07`, planned risk `~99.81`, planned reward `~276.73`, RR `~2.77`. This revised ticket fixed the R:R issue.
- Leverage/margin conclusion for the revised GPT B: do **not** use GPT's `20x`; use about `3x isolated` if placed, with margin `~1056.69 USDT`, fitting the `1500 USDT` max-margin target and leaving liquidation safely below SL. Any placement still requires explicit live confirmation.
- Liquidity/orderability for LLY remained RED due RWA weak-minute/dead-candle issues even when primary stop-exit/depth passed; spread was YELLOW/variable and 24h volume later improved to GREEN. If Andrea confirms placement, require explicit RED-liquidity override.
- Correct final confirmation string provided after correcting the Bitget band mistake: `CONFIRM LLY GPT OPTION B 3X ISOLATED STANDARD LIMITS WITH RED LIQUIDITY OVERRIDE`. No LLY order was placed by tvflow in this sequence; Andrea's manual test order was canceled by Andrea and had no fill.

## LLYUSDT GPT Option B 3x live placement
- Andrea explicitly confirmed `CONFIRM LLY GPT OPTION B 3X ISOLATED STANDARD LIMITS WITH RED LIQUIDITY OVERRIDE` after the earlier Bitget buyLimitPriceRatio misunderstanding was corrected.
- Fresh precheck before placement: LLYUSDT regular/normal/profit_loss state was clean (`0` LLY position, `0` LLY regular open orders, `0` normal plans, `0` profit_loss plans), futures available `~3702.22 USDT`; current LLY reference around last `1159.57`, mark `1159.70`, bid/ask `1159.65/1161.01`.
- Set/verified Bitget LLYUSDT isolated margin mode and explicitly set isolated long/short leverage to `3x`; account verification showed `marginMode=isolated`, `isolatedLongLever=3`, `isolatedShortLever=3` before placement.
- Placed live Bitget standard regular limit long GPT Option B under explicit RED-liquidity override: L1 orderId `1447921771838337025`, clientOid `oc-lly-gptB-3x-20260608-L1`, `0.94 @1135.00`, SL `1092.62`, TP `1200.00`; L2 orderId `1447921772882718721`, clientOid `oc-lly-gptB-3x-20260608-L2`, `1.87 @1124.69`, SL `1092.62`, TP `1240.00`.
- Rounded totals: qty `2.81`, notional `~3170.07`, estimated margin `~1056.69 @3x`, planned risk `~99.81`, planned reward `~276.73`, RR `~2.77`. Liquidity gate was RED but explicitly overridden: stop-exit GREEN, near-depth GREEN, spread YELLOW, p10 weak-minute volume RED, dead candles RED, 24h quote GREEN.
- Postcheck verified two LLY regular orders live, both `baseVolume=0`, `marginMode=isolated`, `leverage=3`, `orderSource=normal`, preset TP/SL attached, no LLY position yet, and no LLY normal/profit_loss plan rows. Account locked margin became `~1069.17` and isolated max available `~2633.05`.
- TradingView Desktop MCP drawing succeeded on `BITGET:LLYUSDT.P` 4H with two solid live Entry/SL/TP ladders and a single white placement arrow. Execution summary saved at `reports/live_execution/20260608_lly_gpt_optionB_3x_live/summary.json`.
