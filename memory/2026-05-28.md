# 2026-05-28 — tvflow durable memory

## AAPLUSDT Case C VOCO watchdog armed
- Andrea requested AAPLUSDT Case C VOCO: 4H close above `313.64`, preferred clean close `>=314.00`, max chase `314.50`, entry `314.20`, qty `37.74`, SL `311.55`, TP `318.00`, planned risk `$100`, RR `1.43`.
- Safety interpretation: VOCO remains alert-only; no live Bitget order was placed. A blind Bitget price trigger would not enforce the required **4H candle close** confirmation, so the safe workflow is the reusable virtual OCO watchdog first, then normal live-order confirmation if it alerts.
- Created `watchdog/virtual_oco_AAPLUSDT_20260528_C.json` with `execution_mode: HYBRID_VOCO`, `risk_cap_usd: 100`, no-chase gate `currentPrice <= 314.50`, and existing live Android AAPL ladder order ids in `liveState.ladderClientOidIncludes` so a C alert warns that A/B ladder exposure must be cancelled/blocked before accepting C unless Andrea separately approves C100.
- Scheduled cron `d1528f70-ed8d-46c3-8a26-ec065855b36b` (`AAPLUSDT.P Case C Virtual OCO Watchdog 4H`) at `5 0,4,8,12,16,20 * * * UTC`, announced to Discord Bitget Trades channel `1499631210283008002`; config scheduler now references the cron id so terminal states can auto-disable it.
- Initial committed check returned `CHECKED` / no trigger: current `311.17`; checked 4H candle `2026-05-28T00:00:00Z`; close `311.34` vs trigger `313.64`; no live order placed.

## Bitget client server-time signing patch
- Authenticated Bitget reads failed with `40008 Request timestamp expired` because host `Date.now()` was roughly `97s` behind Bitget server time.
- Patched `bitget-futures-harness/lib/bitgetClient.js` to fetch `/api/v2/public/time`, cache `serverTime - Date.now()` offset, and sign authenticated requests with server-adjusted timestamps by default (`BITGET_USE_SERVER_TIME`, cache `BITGET_SERVER_TIME_CACHE_MS`). Falls back to local time if the public time call fails.
- Validation: `node --check bitget-futures-harness/lib/bitgetClient.js`; AAPL open-order read returned the 3 existing Android ladder orders; positions read succeeded; AAPL VOCO config JSON parse and watchdog dry/commit checks passed.

## VOCO cron auto-cancel after terminal alert
- Andrea correctly pointed out that the COPPER virtual OCO watchdog cron should be deactivated once the watchdog fires.
- Checked current COPPER VOCO state: `reports/watchdogs/virtual_oco/VOCO_COPPERUSDT_20260526_BC.state.json` is `ALERTED`; triggered family `B DIP_LADDER`; `alertedAt` `2026-05-27T12:07:07.228Z`; subsequent runs return `NO_REPLY` / `watchdog already ALERTED`.
- Disabled active OpenClaw cron `897c8c2b-7c20-4e7a-a650-4adc9b69e7b3` (`COPPERUSDT.P Virtual OCO Watchdog 4H`). Verification after disable: `enabled=false`, `nextRunAtMs=null`, last run `1779941100051`.
- Patched reusable watchdog behavior: `scripts/virtual_oco_watchdog.js` now supports `scheduler.cancelCronAfterTerminal=true` plus `scheduler.cronId`; after a terminal state (`ALERTED`, `EXPIRED`, `INVALIDATED`) is committed, and only when not running with `-NoStateUpdate`, it attempts `openclaw cron disable <cronId>` and includes the result in JSON output.
- Updated `watchdog/virtual_oco_watchdog.template.json` with a scheduler block and patched `watchdog/virtual_oco_COPPERUSDT_20260526_BC.json` to reference the COPPER cron id.
- Updated `README_VIRTUAL_OCO_WATCHDOG.md`: scheduled VOCO configs must set `scheduler.cronId`; commit runs after notification now auto-disable the cron. This keeps DM retry safety (`-NoStateUpdate` before notification) while preventing useless future wakes after terminal alert/expiry/invalidation.
- Validation: `node --check scripts/virtual_oco_watchdog.js` passed; template and COPPER JSON config parse passed.
## VOCO execution modes / risk-cap patch
- Andrea supplied a patch request for the existing reusable VOCO watchdog: add `PURE_VOCO`, `HYBRID_VOCO`, and `HYBRID_C100` support while keeping the tool alert-only and never placing/canceling/modifying orders.
- Patched `scripts/virtual_oco_watchdog.js`: `execution_mode` may be user-specified or inferred from read-only Bitget state. Inference: no live ladder/no C100 => `PURE_VOCO`; live ladder/no C100 => `HYBRID_VOCO`; active C100 plan/order or explicit C100 active => `HYBRID_C100`; unclear state => DM/manual-review alert and no trade proposal.
- Added `risk_cap_usd` with default `100`; risk gates now compare against `risk_cap_usd` and fail when planned risk cannot be calculated/verified (`risk verification failed � refresh/resize required`).
- Added read-only live-state reporting: ladder status (`no live ladder found`, `ladder open/unfilled`, `ladder partially filled`, `ladder fully filled`, unknown) and C100 status (`C100 plan/order active`, none, unknown).
- Added HYBRID behavior: `HYBRID_VOCO` C alerts warn that ladder exposure must be cancelled/blocked through the normal live-order workflow before accepting C; partial/full ladder states produce manual-review alerts instead of normal C proposals. `HYBRID_C100` C alerts require combined ladder+C risk verification and report C100 check pass/fail.
- Updated every trade-critical DM format to include execution mode/source, risk cap, ladder/C100 status, triggered family, blocked/coexisting family, and risk mode; final action line says order handling must use the normal live-order tool/workflow and that the watchdog did not place or cancel live orders.
- Updated `watchdog/virtual_oco_watchdog.template.json`, COPPER config, and `README_VIRTUAL_OCO_WATCHDOG.md` with the new mode/risk/c100 fields.
- Validation: `node --check scripts/virtual_oco_watchdog.js`; template and COPPER JSON parse; forced no-state COPPER dry check returned `CHECKED` without committing state; `git diff --check` passed.
