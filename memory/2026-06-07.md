# 2026-06-07

## VOCO live-fill routing patch
- Andrea clarified VOCO Discord DM behavior: DM only when a virtual order/branch triggers and needs attention.
- When the actual real/live order leg fills, the VOCO should cancel/disarm the remaining virtual alternative and send only a Bitget Trades room update, not a DM.
- Patched `scripts/virtual_oco_watchdog.js`: `HYBRID_VOCO` with live ladder status `PARTIALLY_FILLED` or `FULLY_FILLED` now returns terminal `LIVE_LEG_FILLED`, message `VIRTUAL OCO LIVE LEG FILLED — VIRTUAL ALTERNATIVE CANCELLED`, and `notificationKind=room`.
- This cancellation is watchdog/virtual-state only; the tool remains read-only and never places/cancels/modifies exchange orders. Terminal cron-disable now includes `LIVE_LEG_FILLED`.
- Updated `README_VIRTUAL_OCO_WATCHDOG.md`, `watchdog/virtual_oco_watchdog.template.json`, and GE/CL cron prompt files so scheduled routing sends virtual triggers to DM but live-fill resolution to the room only.
- Validation passed: `node --check scripts\virtual_oco_watchdog.js`; forced CL dry-run with `-NoStateUpdate -Force` returned `LIVE_LEG_FILLED`, ladder `FULLY_FILLED`, position size `37.5`, and room-only notification kind.

## Obsidian tvflow vault migrated to Google Drive
- Andrea asked to put the tvflow Obsidian vault in `G:\My Drive\Trading` so it is visible and usable from other machines.
- Migrated the vault from `C:\Users\anmar\.openclaw\workspace-tvflow\obsidian-vault-tvflow` to canonical shared path `G:\My Drive\Trading\obsidian-vault-tvflow`.
- Preserved OpenClaw compatibility by renaming the original local folder to `C:\Users\anmar\.openclaw\workspace-tvflow\obsidian-vault-tvflow.local-backup-20260607-205304` and creating a Windows junction at the old path pointing to the Google Drive vault.
- Verified required vault files copied: `.obsidian`, `00_Index\Home.md`, and `00_Index\OpenClaw Usage Rules.md`; target count after migration was 11 files and 9 directories.
- Updated `PROJECT_STATE.md` and vault index/usage notes to make the Google Drive path the canonical vault location. The vault remains a curated wiki/memory layer, not a live-order authorization source or automatic rule engine.

## CLUSDT HYBRID_VOCO duplicate-B alert hardening
- Andrea reported the CL ladder had filled and he received a VOCO DM, asking why VOCO was still armed.
- Read-only Bitget check showed CLUSDT full GPT-B short position filled: `37.5 @92.683333333332`, isolated `20x`, no regular open orders, and four live Bitget `profit_loss` TP/SL plan rows for the filled legs. No exchange orders were placed/cancelled/modified during the investigation.
- Root cause: the HYBRID_VOCO config included both the already-live GPT-B pullback family and virtual C2. On the 2026-06-06 20:00 UTC closed 4H candle, the watchdog saw the B entry zone touched while the ladder was `PARTIALLY_FILLED` and sent a `B SELL_RALLY` DM proposal. C2 did not trigger. This was a duplicate/noisy B alert because Bitget was already managing the live B ladder.
- Immediate fix: disarmed `VOCO_CLUSDT_20260605_GPTB_C2` state with `run_virtual_oco_watchdog.ps1 -Mode disarm`; state now `DISARMED`.
- Code hardening: patched `scripts/virtual_oco_watchdog.js` so `HYBRID_VOCO` suppresses duplicate pullback/A-B alerts by default when read-only live state detects a matching ladder/order/position (`OPEN_UNFILLED`, `PARTIALLY_FILLED`, or `FULLY_FILLED`). Added template flag `suppressPullbackAlertsWhenLiveLadderDetected: true` and README guidance.
- Verification: `node --check scripts\virtual_oco_watchdog.js` passed; forced CL dry-run with `-NoStateUpdate -Force` now reports B suppressed due `FULLY_FILLED` and C2 waiting/no-trigger.
