# 2026-06-17

## GEUSDT refresh check after prior TP1 / only L1 filled
- Andrea reported GEUSDT.P orders hit TP1 and only L1 was filled, then asked whether the ticket could be refreshed. Read-only Bitget check confirmed the prior GPT B L1 `3.42 @340.60` filled and later closed by `profit_market` around `347.43`, gross `+23.3658 USDT`; B2/B3 from that older GPT ladder were canceled; no GE position is currently open.
- Current GE exposure is only the 2026-06-16 refreshed A1/A2 resting ladder, both still live and unfilled (`baseVolume=0`): A1 `4.44 @344.20`, SL `335.20`, TP `356.00`; A2 `11.11 @340.60`, SL `335.20`, TP `352.40`; no GE normal/profit_loss/track plans.
- Fresh packet/screenshots show GE extended near `357`, above the old `347.8-349.5` daily high. Nearest support stack is `352-350`, `348.5`, then `344-340`; the existing A1/A2 ladder remains structurally relevant as a deeper post-breakout pullback ladder.
- A higher shallow refresh around `352.5/351.1` is structurally closer but poor R:R with a proper structural SL below `341.7`; liquidity for both existing and hypothetical shallow refresh remains RED despite stop-exit/depth GREEN because spread/p10/dead-candles/24h quote volume are RED.
- Default recommendation given: do not chase/replace higher now; keep existing A1/A2 unless Andrea explicitly asks to cancel/replace under a RED-liquidity override. Any cancellation/replacement/live placement still requires separate explicit confirmation. Summary: `reports/live_execution/20260617_ge_refresh_check_summary.md`.
